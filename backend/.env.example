# Copy file ini ke backend/.env (yang ter-gitignore), lalu isi nilainya.
# Atau export langsung di shell sebelum run: ./mvnw spring-boot:run
#
# File .env TIDAK otomatis dibaca Spring Boot. Cara pakai:
#   1. Setelah copy & isi, source manual:  set -a; source .env; set +a
#   2. Atau export satu per satu di terminal

# App Password Gmail 16 digit untuk akun heyjipro@gmail.com
# Generate di: https://myaccount.google.com/apppasswords (aktifkan 2FA dulu)
MAIL_PASSWORD=abcd-efgh-ijkl-mnop
